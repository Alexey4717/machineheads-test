import { Typography } from 'antd';

export default function HomePage() {
  return (
    <div>
      <Typography.Title level={2}>Admin Panel</Typography.Title>
      <Typography.Paragraph>
        Home page stub — business logic will be added later.
      </Typography.Paragraph>
    </div>
  );
}
