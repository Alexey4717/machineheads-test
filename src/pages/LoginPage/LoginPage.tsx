import { Typography } from 'antd';

export default function LoginPage() {
  return (
    <div>
      <Typography.Title level={2}>Login</Typography.Title>
      <Typography.Paragraph>Login page stub — auth form will be added later.</Typography.Paragraph>
    </div>
  );
}
